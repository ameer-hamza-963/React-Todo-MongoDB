version: '3.7'

services:
  server:
    build:
      context: ./Server
      dockerfile: Dockerfile
    image: myapp-server
    container_name: myapp-node-server
    command: node server.js
    ports:
      - '4000:4000'
    environment:
      - MONGODB_URI=mongodb+srv://ameerhamza963:Eo0mIqr2IvOkn7Yl@cluster0.ylla0w5.mongodb.net/
      - NODE_ENV=development
      - API_URL=http://ec2-54-82-10-46.compute-1.amazonaws.com:4000

  client:
    build:
      context: ./Web
      dockerfile: Dockerfile
    image: myapp-client
    container_name: myapp-react-client
    depends_on:
      - server

volumes:
  data-volume:
  web-root:
    driver: local
